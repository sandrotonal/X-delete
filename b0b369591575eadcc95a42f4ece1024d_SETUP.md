# Kurulum Rehberi

## 1. Sistem Gereksinimleri
- Node.js 14.0.0 veya üzeri
- npm (Node Package Manager)
- Twitter Developer hesabı

## 2. Projeyi İndirin
```bash
# Eğer git ile klonladıysanız
git clone <repository-url>
cd twitter-cleanup

# Veya dosyaları manuel olarak indirin
```

## 3. Bağımlılıkları Yükleyin
```bash
npm install
```

## 4. Twitter API Kurulumu

### 4.1 Developer Hesabı Oluşturun
1. [developer.twitter.com](https://developer.twitter.com) adresine gidin
2. "Apply for a developer account" butonuna tıklayın
3. Hesap bilgilerinizi doldurun
4. Kişisel kullanım amacınızı belirtin

### 4.2 Uygulama Oluşturun
1. Developer Portal'da "Create Project" tıklayın
2. Proje adı ve amacı girin
3. "Twitter API v2" seçeneğini etkinleştirin
4. Uygulamanızı oluşturun

### 4.3 API Anahtarlarını Alın
1. Uygulama ayarlarına gidin
2. "Keys and tokens" sekmesine geçin
3. Aşağıdaki bilgileri kopyalayın:
   - **API Key (Consumer Key)**
   - **API Secret (Consumer Secret)**
   - **Access Token**
   - **Access Token Secret**

### 4.4 OAuth 1.0a Etkinleştirin
1. Uygulama ayarlarında "User authentication settings" bölümüne gidin
2. OAuth 1.0a'yı etkinleştirin
3. Callback URI'yi ayarlayın (örn: `http://localhost:8080/callback`)

## 5. Ortam Değişkenlerini Ayarlayın

### 5.1 .env Dosyası Oluşturun
```bash
cp .env.example .env
```

### 5.2 API Anahtarlarınızı Girin
`.env` dosyasını bir metin editörü ile açın ve Twitter API anahtarlarınızı girin:

```env
API_KEY=your_actual_api_key_here
API_SECRET=your_actual_api_secret_here
ACCESS_TOKEN=your_actual_access_token_here
ACCESS_SECRET=your_actual_access_token_secret_here
```

**⚠️ Önemli**: 
- `your_actual_api_key_here` kısımlarını gerçek anahtarlarınızla değiştirin
- `.env` dosyasını asla git'e commit etmeyin
- `.gitignore` dosyanızda `.env` olduğundan emin olun

## 6. İlk Test

### 6.1 Bağlantı Testi
```bash
node index.js --dry-run
```

### 6.2 Beklenen Çıktı
```
🧹 Twitter Tweet Temizleme Aracı
========================================
🔐 Twitter API bağlantısı kuruluyor...
✅ Bağlantı başarılı! Kullanıcı: @kullaniciadi
📊 Tweetler çekiliyor...
...
```

## 7. Hata Durumları ve Çözümleri

### 7.1 "Cannot find module 'twitter-api-v2'"
```bash
npm install twitter-api-v2 dotenv
```

### 7.2 "Unauthorized" Hatası
- API anahtarlarınızı kontrol edin
- OAuth 1.0a'nın etkin olduğundan emin olun
- Access token'ların doğru olduğunu kontrol edin

### 7.3 "Forbidden" Hatası
- Twitter Developer hesabınızın onaylanmış olduğundan emin olın
- API planınızın tweet silme yetkisine sahip olduğunu kontrol edin

### 7.4 Rate Limit Hatası
- Normal bir durum, script otomatik bekler
- Sabırlı olun ve script'in çalışmasını bekleyin

## 8. Güvenlik Kontrol Listesi

- [ ] `.env` dosyası gitignore'da
- [ ] API anahtarları güvenli yerde saklanıyor
- [ ] OAuth 1.0a etkin
- [ ] Sadece kendi hesabınız için kullanıyorsunuz

## 9. Artık Hazırsınız!

Kurulum tamamlandı. Şimdi [README.md](README.md) dosyasındaki kullanım örneklerini takip edebilirsiniz.

**İlk çalıştırma için mutlaka dry-run modunu kullanın:**
```bash
node index.js --dry-run
```